package no.skatteetaten.aurora.boober.model

object Paths {

    const val basePath = "/u01"
    const val logPath = "$basePath/logs"
    const val binPath = "$basePath/bin"
    const val secretsPath = "$basePath/secrets/app"
    const val configPath = "$basePath/config"
}
