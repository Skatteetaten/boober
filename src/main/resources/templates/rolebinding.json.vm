{
  "kind": "RoleBinding",
  "apiVersion": "v1",
  "metadata": {
    "name": "admin"
  }
#if (${adc.groups.size()} != 0)
,"groupNames": [
  #foreach ($group in $adc.groups)
    #if($velocityCount != 1)
      ,
    #end
    "${group}"
  #end
]
#end

#if (${adc.users.size()} != 0)
,"userNames": [
  #foreach ($user in $adc.users)
    #if($velocityCount != 1)
      ,
    #end
    "${user}"
  #end
]
#end

#if (${adc.rolebindings.size()} != 0)
,"subjects": [
  #foreach ($binding in $adc.rolebindings.entrySet())
    #if($velocityCount != 1)
      ,
    #end
  {
    "kind": "$binding.value",
    "name": "$binding.key"
  }
  #end
]
#end

, "roleRef": {
"name": "admin"
}
}
