{
  "kind": "ConfigMap",
  "apiVersion": "v1",
  "metadata": {
    "name": "${adc.name}",
    "labels": {
      "app": "${adc.name}",
      "updatedBy" : "${username}",
      "affiliation": "${adc.affiliation}",
      "booberDeployId": "${deployId}"
    }
  },
"data": {
  #foreach ($config  in $adc.config.entrySet())
    #if($velocityCount != 1)
      ,
    #end
    "${config.key}": "${config.value}"
  #end
}
}
