{
  "kind": "ConfigMap",
  "apiVersion": "v1",
  "metadata": {
    "name": "${app.name}",
    "labels": {
      "app": "${app.name}",
      "updatedBy" : "${app.username}",
      "affiliation": "${app.affiliation}"
    }
  },
  "data": {
    "latest.properties": "${appConfig}"
  }
}
