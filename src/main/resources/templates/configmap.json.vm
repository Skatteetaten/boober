{
  "kind": "ConfigMap",
  "apiVersion": "v1",
  "metadata": {
    "name": "${adc.name}",
    "labels": {
      "app": "${adc.name}",
      "updatedBy" : "${username}",
      "affiliation": "${adc.affiliation}"
    }
  }
  #if (${configMap})
  ,"data": {
    "latest.properties": "${configMap}"
  }
  #end
}
