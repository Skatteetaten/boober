{
  "kind": "Route",
  "apiVersion": "v1",
  "metadata": {
    "name": "${adc.name}",
    "labels": {
      "app": "${adc.name}",
      "updatedBy" : "${username}",
      "affiliation": "${adc.affiliation}"
    }
  },
  "spec": {
    "to": {
      "kind": "Service",
      "name": "${adc.name}"
    }
  }
}
