{
  "kind": "ImageStream",
  "apiVersion": "v1",
  "metadata": {
    "name": "${app.name}",
    "labels": {
      "app": "${app.name}",
      "updatedBy" : "${app.username}",
      "affiliation": "${app.affiliation}"
    }
  },
    #if (${app.type} == "deploy")
      "spec": {
        "dockerImageRepository": "${docker.registry}",
    "tags": [
          {
              "name": "${docker.istag}",
              "from": {
                "kind": "DockerImage",
                "name": "${docker.registry}:${docker.tag}"
              },
              "importPolicy": {
                "scheduled": true
              }
            }     ]
      }
    #end
}
