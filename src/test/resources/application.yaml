spring:
    main:
        banner-mode: "off"
    application:
        name: boober

aurora:
    token:
        value: token

integrations:
    bitbucket:
        username: "foo"
        password: "bar"
        url: "https://git.aurora.skead.no/"
    aurora:
        config:
            git:
                project: "ac"
                urlPattern: "file:///tmp/boobertest/%s"
                checkoutPath: "build/boobercheckout"
        deployLog:
            git:
                project: "ao"
                repo: "auroradeploymenttags"
        vault:
            git:
                urlPattern: "file:///tmp/vaulttest/%s"
                checkoutPath: "build/vaultcheckout"
    openshift:
        url: https://${openshift.cluster}-master.paas.skead.no:8443
    dbh:
        url: "http://localhost:8080"
    skap:
        url: "http://localhost:8082"
    docker:
        registry: "docker-registry.aurora.sits.no:5000"
        releases: "docker-registry.aurora.sits.no:5000"


boober:
    encrypt:
        key: "komogsyngensang"
        releases: "docker-registry.aurora.sits.no:5000"
        token: "test"
    openshift:
        token: foo

vault:
    operations:
        enabled: true
    git:
        urlPattern: "file:///tmp/vaulttest/%s"
        checkoutPath: "build/vaultcheckout"
        username: "foo"
        password: "bar"

management:
    server.port: 0

openshift:
    cluster: utv
    url: https://${openshift.cluster}-master.paas.skead.no:8443
